# 页面词语解释

常用设置已经改成简单说法。括号里的旧词方便你对照游戏菜单或以前的说明；不知道鼠标档位可以留空。下面也解释了公式和测试报告里的词，平时不用记。

这份表对应页面、使用说明和测试报告。原始备份中的英文项目名称保留，以兼容旧记录。

## 画面和鼠标设置

| 原来的词 | 简单说法 | 意思／怎么填 |
|---|---|---|
| FOV、视野 | 画面能看到的范围 | 数字通常越大，能看到的周围场景越多。照抄游戏里的数值，不是鼠标转动快慢。 |
| 水平 FOV | 左右能看到的范围 | 从画面最左边到最右边的可见范围。 |
| 垂直 FOV | 上下能看到的范围 | 从画面最上边到最下边的可见范围，不能直接拿左右范围的数字代替。 |
| 16:9 基准水平 FOV | 以 16 : 9 画面为准的左右范围 | 先按 16 : 9 理解游戏的数字，再按你的游戏画面比例换算。程序负责计算；特殊画面仍需核对。 |
| 宽高比、画幅、aspect | 画面的宽和高之比 | 例如 1920 × 1080 的画面是 16 : 9。游戏有黑边或开在窗口里时，可能与屏幕比例不同。 |
| 分辨率 | 画面横着和竖着各有多少个小点 | 例如 1920 × 1080。它表示画面大小，不是游戏里能看到多广。 |
| 像素、px | 组成画面的小点或显示单位 | 屏幕实际像素是显示器的小点；网页使用的单位不一定与它一一对应。 |
| CSS 像素 | 网页使用的大小单位 | 例如网页宽度 1280，指网页自己的显示单位，不一定代表 1280 个屏幕实际像素。 |
| DPR、devicePixelRatio | 屏幕显示比例 | 一个网页显示单位对应多少屏幕实际像素。会受系统和网页缩放影响；不是鼠标 DPI，也不能单独代表 Windows 缩放百分比。 |
| viewport、网页可见区域 | 浏览器里实际用来显示网页的区域 | 不包含地址栏等区域。它的大小不是游戏画面大小。 |
| DPI | 鼠标自身的灵敏度档位 | 鼠标移动 1 英寸（2.54 厘米）时报告多少移动计数。常见如 800、1600；与游戏内灵敏度是两项设置，不知道可留空。 |
| eDPI | 鼠标档位与游戏灵敏度的乘积 | 例如 800 × 5 = 4000，只作比较用；4000 不是游戏里要填写的灵敏度。 |
| 回报率、polling rate、Hz | 鼠标每秒向电脑报告位置的次数 | 例如 1000 Hz 代表每秒约报告 1000 次。网页收到消息的次数不一定等于这个数字。 |
| 鼠标加速 | 同样距离，甩得快时可能转得更多 | 移动速度也影响转动量。这会干扰只按鼠标移动距离换算的结果。 |
| 原始输入、raw input、unadjustedMovement | 未被系统加速改动的鼠标数据 | 程序会尝试读取，但浏览器请求成功不等于硬件已验证，仍需用尺子实测。 |
| Pointer Lock、鼠标锁定 | 让鼠标控制视角 | 开始后鼠标箭头隐藏，移动鼠标像游戏里一样转动视角。按键盘左上角 Esc 键退出，本轮需重测。 |
| 基础灵敏度 | 游戏里平时转动视角的灵敏度 | 本工具最终只推荐这一项，供所有英雄共用，不替你改其他设置。 |
| 相对灵敏度、倍率 | 开镜或使用技能时的鼠标速度比例 | 控制特殊状态与平时转动速度的关系。测试期间维持你原来的设置。 |
| 全局设置、英雄覆盖 | 所有英雄共用设置、某个英雄单独的设置 | 如果某个英雄单独设置过基础灵敏度，它可能不再跟随共用值；需要逐项检查这一项，不重置按键。 |
| cm/360、转距 | 视角转一圈，鼠标要移动多少厘米 | 例如 30 厘米/圈。用尺子测鼠标实际移动距离，不是测屏幕上的光标距离。 |
| cm、ms、° | 厘米、毫秒、度 | 1 cm 是 1 厘米；1000 ms 是 1 秒，所以 150 ms 是 0.15 秒；90° 是四分之一圈，180° 是半圈，360° 是一圈。 |

## 测量和测试安排

| 原来的词 | 简单说法 | 意思／怎么填 |
|---|---|---|
| 校准、物理校准 | 用尺子测量，让网页与游戏对齐 | 把真实鼠标移动距离与视角转动量对应起来。不是自动识别鼠标 DPI，也不是直接修改鼠标。 |
| 采样、双轴采样 | 记录鼠标移动、分别记录左右和上下移动 | 从尺子起点移动指定距离，记录这一趟。每次按空格开始和结束，中途不要抬鼠或往返。 |
| 线性、非线性 | 距离与转动量是否成比例 | 同一设置下，移动距离翻倍，转动量也应接近翻倍；相同距离快慢移动应得到接近结果。明显不符就重测。 |
| 保存精度、接受精度 | 游戏能真正保存几位小数 | 输入后关闭设置再打开，看数字有没有变。网页只比较真正保存并测试过的数值。 |
| 取整、去重 | 按能保存的小数位数处理，去掉重复值 | 如果两档在游戏里都保存成同一个数字，就只保留一档。 |
| 预试、适应 | 不计分练习、先熟悉当前灵敏度 | 练习用于选合适难度，适应用于熟悉这一档设置。这两种成绩都不用于推荐。 |
| 冻结参数 | 把难度和规则固定下来 | 正式比较期间不改变目标大小、速度、发射节奏等，否则成绩不能公平比较。 |
| 候选 | 准备拿来比较的设置 | 例如原设置是 5，就先测试 4、5、6.25；最终只给一个结果，证据不足就保留 5。 |
| 基线 | 原来使用的设置及其成绩 | 所有其他设置都与同一天原设置的表现比较。不是把所有英雄各自最好的数值平均。 |
| 批次、比较组 | 使用同一套条件的一批比较记录 | 设备、测量结果、难度和规则保持一致。换条件后另开一组，旧成绩不偷偷混进去。 |
| 会话、session、round | 今天这一组测试、其中某一轮任务 | 一组通常包含三档设置各做四类任务；一轮是其中一个计分任务。 |
| 随机种子、seed | 生成同一套目标路线的编号 | 用相同编号能重现相同路线，避免某个设置总遇到更容易的目标。编号不用你填写。 |
| 探索、确认、相邻候选 | 先找方向、再试一次、试附近的数字 | 初次比较找可能合适的数值；之后可另开比较组，测试它附近的数值，同时保留原设置。 |
| 跨日复测、有效日期 | 换日期再测、当天记录够完整才能用于比较 | 一天做多次仍只算一天。至少 3 个不同日期；缺任务或原设置零分等情况可能无法比较。 |
| 成对比较、区组设计 | 同样条件下，把原设置和测试设置放在一起比 | 同一天、同样任务与难度下对比，尽量减少条件变化的影响。 |
| 等权、权重 | 四类任务同样重要 | 每一类在总体结果里占同样份量，不能只靠某一项特别好就推荐。 |
| 均值、极差 | 平均值、最大值与最小值的差 | 平均值是加起来再除以次数；最大最小之差表示这几次测量相差多大。 |
| 相对提升、百分比 | 比原设置提高或降低了多少 | 原来完成 20 次，现在 22 次，就是提高 10%；不是提高 10 个百分点。 |
| 阈值、工程门槛 | 程序采用的判断规则 | 例如总体平均提高必须超过 3%，又不能有明显单项下降。这是保守规则，不是效果保证。 |
| 统计显著性 | 经过统计检验，判断差异是否可能只是偶然 | 本工具没有因为测够 3 天或达到某个百分比，就宣称已经证明效果。 |
| 短板、舒适度 | 某类明显变差、使用起来是否舒服 | 转身困难、撞到鼠标垫边或手臂不舒服都要如实记录，不靠网页假装自动识别。 |

## 任务里发生的事

| 原来的词 | 简单说法 | 意思／怎么填 |
|---|---|---|
| 准星、第一人称视角 | 屏幕中心的小十字、像游戏中角色一样看四周 | 鼠标控制视角转动，准星留在中心，不是用桌面箭头点击圆圈。 |
| 首次捕获、停稳、停留阈值 | 第一次碰到目标、在目标里保持一小段时间 | 第一次碰到不等于停住。停稳任务要求连续保持 0.15 秒，离开就重新累计。 |
| 跟枪、角速度 | 跟着移动目标瞄准、目标在视野里移动的快慢 | 尽量把准星留在目标里。这里自动计算，不需要点击开火。 |
| 跟踪时间比、时间加权 | 准星在目标里的时间占比、按持续时间计算 | 例如 10 秒里有 6 秒瞄住，就是 60%。按时间算，不按鼠标消息或画面数量算。 |
| 角误差、恢复时间 | 准星偏离目标多远、跟丢后多久重新瞄上 | 用来帮助理解表现；这里的角误差用角度表示，不是屏幕距离。 |
| 弹道、射弹、弹丸、下坠 | 子弹飞行路线、飞出的子弹、飞行中向下落 | 子弹需要时间才能到目标那里，不会在发射瞬间命中，也不会自动追踪目标。 |
| 提前量 | 瞄准目标将要到达的位置 | 目标在移动，子弹飞过去也需要时间，所以常要瞄在目标前面；子弹下落时还可能要稍微瞄高。 |
| 重新定位、目标切换 | 转身或移动后重新瞄准、换下一个目标 | 看方向提示转身，再把准星放回目标里，不是只转够角度就算完成。 |
| 有效时间、结算、超时 | 真正计分的时间、等最后结果、规定时间内未完成 | 练习和中断时间不计分；计时结束后不再发射，只等待已经飞出的子弹打中或飞行时间到期。 |
| 帧率、FPS、帧 | 每秒画面更新多少次、一张画面 | 这里的 FPS 若指帧率，是每秒更新次数；若指游戏类型，则是第一人称射击游戏。画面刷新次数不应改变鼠标转角。 |
| 扫掠相交、插值、积分、透视投影 | 检查中途经过的位置、按时间计算运动、把远近画面画到屏幕上 | 避免子弹或准星很快划过目标时漏判。这些是程序内部的近似计算，你不用设置。 |
| 失焦、切后台、中断 | 切到其他窗口或页面、这一轮停止计分 | 按 Esc、切窗口或明显卡顿后，本轮标记无效，回到网页从头重测这一轮。 |
| 过冲、吸附、平滑、瞄准修正 | 甩过头、自动吸住目标、把移动变柔和、自动帮你瞄 | 本工具不偷偷加吸附或瞄准修正；A 也不把目标消失后的鼠标移动当成甩过头。 |

## 保存、公式与排错说明

| 原来的词 | 简单说法 | 意思／怎么填 |
|---|---|---|
| 只读、离线、本地 | 只查看不修改、不联网、留在这台电脑的浏览器里 | 环境检测不会改系统或游戏设置，程序运行不依赖网络；保存记录仍会写入浏览器自己的存储。 |
| HTML、file:// | 可以直接打开的网页文件、电脑上的文件地址 | 双击 HTML 用 Chrome 或 Edge 打开即可，不需要启动网站服务。 |
| JSON、导出、导入 | 完整备份文件、下载备份、从备份恢复 | 完整备份保存设置和成绩。恢复前会检查文件并请你确认；确认后替换当前记录。 |
| CSV | 成绩表文件 | 可用 Excel 等表格软件查看。它是方便查看的表格，不能替代可恢复全部设置的完整备份。 |
| 本地存储、配额、持久化 | 浏览器保存记录的位置、可用空间、关闭后仍保留 | 空间不足会提醒。换浏览器、移动文件或清理数据可能看不到旧记录，所以要下载备份。 |
| 轨迹、原始成绩、排除理由 | 详细鼠标移动记录、实际完成数和时间、为什么不算这一轮 | 详细移动记录仅保留最近 24 轮，汇总成绩保留。中断等原因会记下来，不把记录悄悄删掉。 |
| S、S₀、C₀、B、yaw、Δyaw | 公式里给数值起的短名字 | S 是测试灵敏度；S₀ 是原灵敏度；C₀ 是原设置转一圈的厘米数；B 是每厘米网页记录的移动量；yaw 是左右转角，Δyaw 是本次改变了多少。 |
| 0.0066 转向系数 | 旧资料里把鼠标移动量换成游戏转角的比例 | 只用于额外核对，不保证当前游戏版本一定相同。实测距离更重要。 |
| schema、version、calibrationVersion、taskVersion | 备份格式版本、程序或测量规则的版本编号 | 帮助程序识别能不能读取和比较这些记录。不是你的鼠标型号，也不用手动填写。 |
| sessionId、batchId、id、date、seed | 测试编号、比较组编号、记录编号、日期、路线编号 | 完整备份中的英文项目名称。用于把记录分清楚，不是额外需要输入的设置。 |
| params、rules、raw、valid、reason、trace | 任务设置、判断规则、实际成绩、是否有效、排除理由、移动记录 | 这些英文只保留在排错记录和备份文件里，避免旧备份不能读取。日常操作看中文页面即可。 |
| requested、fallback、legacy | 请求未加速数据、使用普通数据、浏览器不能确认支持情况 | 输入方式的内部名称。页面已经显示中文说明，三者都不自动证明鼠标硬件输入已验证。 |
| API、mousemove、movementX、movementY | 浏览器提供的功能、鼠标移动消息、左右和上下移动量 | 开发时使用的名称。网页每条鼠标移动消息只记一次，不把消息数量当成硬件回报率。 |
| performance.now、requestAnimationFrame | 持续走动的计时器、浏览器安排下一次画面更新 | 计时器决定经过多少时间，画面更新只负责显示；鼠标不动时也应正常结束。 |
| 浮点误差 | 电脑计算小数时出现的极小差别 | 例如数学上刚好 3%，电脑可能算成略多一点点。程序会避免这种误差误触推荐门槛。 |
| headless、冒烟测试、故障注入、测试夹具 | 不显示窗口的测试、基本检查、故意模拟出错、专用测试数据 | 这些是测试报告中的开发用词。本项目的桌面浏览器验收显示窗口；模拟的鼠标和成绩不能当成你的真人实测。 |
| Aimlabs、KovaaK、MDN、NIST | 训练工具或参考资料的名称 | 它们是名称，不是需要填写的参数。引用其资料不代表本工具是官方产品或推荐效果得到保证。 |
