# 实际验收记录

阅读提示：这里保留了开发验收时的原始测试名称。看不懂的专业词可查 [术语说明.md](术语说明.md)，共 72 项；日常操作请看已经改为简单说法的 [使用说明.md](使用说明.md)。

验收日期：2026-09-20。交付文件：`overwatch-sensitivity.html`。运行文件无需依赖测试脚本。

使用本机已安装的 **Windows 桌面 Chrome 153.0.8010.48** 和 **Edge 153.0.4234.48**，均以有窗口模式（非 headless）打开 `file:///D:/ow灵敏度/overwatch-sensitivity.html`。浏览器测试上下文设置为断网，页面没有请求 HTTP/HTTPS 资源。已查看设置页、结果页和训练画面的截图。

测试使用独立的临时浏览器配置。校准、跨日成绩和游戏复核所需的数据是明确的测试夹具，未写入交付 HTML 或用户的日常浏览器配置。它们只能证明程序分支正确，不能证明用户的鼠标参数或游戏表现。

## 自动测试已通过

| 层次 | 实际结果 | 证据 |
|---|---|---|
| 核心与扩展算法 | **28 / 28 通过**，其中 20 项可从 HTML 结果页运行 | `tests/core-test-results.json` |
| Chrome 桌面交互 | **18 / 18 通过** | `tests/browser-test-results.json` |
| Edge 桌面交互 | **18 / 18 通过** | 同上 |
| 完整业务流程 | **6 项通过**，另记录真实墙钟时长 | `tests/workflow-test-results.json` |
| 最终文件冒烟及故障分支 | **Chrome 5 / 5，Edge 5 / 5 通过** | `tests/final-smoke-results.json` |

覆盖的验收项目：

- **计时**：不发送鼠标移动，实际等待完整 45 秒的 A 模块，自动结束并保存；从测试端开始等待到结束为 **44,988 ms**（测试端在引擎启动后才开始计时）。其余完整流程使用时钟偏移加速，报告没有把它们算作真实 12 分钟体验。
- **输入与碰撞**：快速扫过只完成旧目标一次；新目标不在准星内；首次触碰与连续停稳 150 ms 不混同；静止准星也能判定移动目标经过。相同时间戳输入在不同刷新推进频率下转角一致，不重复乘 DPI；跟枪时间与角误差的 30/60/144 Hz 数值比较在测试容差内。
- **弹道**：发射瞬间方向固定，后续转向不修正弹丸；目标在发射时位于准星不等于飞行后命中；高速射弹用运动路径碰撞；结束不新增发射，末尾弹丸命中或超时全部结算。
- **公平比较**：路线种子可重放，三档同日各模块的种子匹配；六种排列平衡候选位置；三档四类整组能完成，适应与延长另行记录，不计分；同日重复不增加日期数。
- **保守推荐**：同分保留原值；恰好 3% 不因浮点误差越过“超过 3%”门槛；单项明显受损、零基线及反复不适不强行推荐；合成的三日均衡优势能进入网页初选；游戏复核失败退回原值，通过只接受同一已测值；相邻确认批次不合并旧探索成绩。
- **校准与保存值**：缺失校准、非线性、方向／速度不合格、另一档转距不符会拒绝；候选按游戏接受精度取整去重，必须保留原基线；未知 DPI 不虚构理论值。
- **浏览器中断**：实际 Pointer Lock 请求和程序的 Esc 退出路径通过；正式轮次中断有排除理由，重测从零开始；失焦事件与超过 250 ms 的计时停顿均中断；重复开始不会生成重复会话。失焦测试包含事件注入，不等于全面覆盖所有系统切换行为。
- **失败路径**：原始输入不支持时提示再次点击普通锁定；权限拒绝和无 Pointer Lock 时不开始计分。拒绝条件使用故障注入，普通锁定使用真实浏览器 API。保存失败提示不会被后续成功提示覆盖。
- **文件与持久化**：离线加载，JSON 下载和完整副本导入，非法参数和恶意对象字段拒绝，CSV 中文与原始字段，刷新恢复记录但不续跑半轮；不同校准和任务版本不静默合并。

**最终运行失败项：无。** 上述结论限于已列测试覆盖范围；没有把开发过程中的失败尝试或合成分数当作用户实测记录。

## 待你用真实鼠标和游戏验证

| 项目 | 人工步骤与通过标准 |
|---|---|
| 游戏原设置转距 | 用远处固定地标，左右、快慢各测一次 cm/360；分段累计须标记。四次极差不超过均值 10%，否则先重测。 |
| 网页实际输入 | 同一鼠标和 DPI，用尺子完成水平、垂直正反及快慢 8 次。慢速每厘米耗时至少为快速的 1.5 倍；各次位移单位/cm 极差不超过均值 10%。这仍不证明拿到了全部硬件采样。 |
| 另一档游戏核对 | 临时设置另一个已保存数值，测转距，对比预测误差不超过 10%，然后恢复原值。 |
| 当前客户端输入范围与精度 | 逐档输入、关闭并重开设置，记录实际保存值、小数位及允许范围。开发时没有代替你进入当前游戏客户端验证。 |
| 基础覆盖与相对倍率 | 检查全局及英雄单独的基础灵敏度覆盖；只统一基础项，保留按键和开镜／技能／形态倍率。 |
| FOV 与画幅 | 默认使用 16:9 基准水平 FOV 转换。特殊宽高比、裁切或客户端变化应实际核对；详情可切换实际水平／垂直定义。 |
| 系统输入行为 | 验证鼠标真实距离、系统加速、浏览器锁定和 Esc/Alt-Tab/切后台的实际体验。不要把 API 请求成功视为原始输入已经验证。 |
| 真实操作与舒适度 | 实际完成四类预试与正式测试；转身抬鼠、撞垫边和不适由你反馈，网页不会自动识别。 |
| 跨日稳定性 | 至少 3 个真实不同日期，各日都完成整组三档；建议至 6 天。日期和阈值本身不构成统计显著性或准确率保证。 |
| 游戏最终复核 | 对持续跟枪、精确射击/开镜、弹道、近身转向、位移后定位做同英雄同条件两轮成对比较；核对候选保存值及最终 cm/360。可重复短板退回原值。 |

## 已公开的设计限制

- 所有任务都是原创训练参数，不能代表某个具体英雄的完整机制。未验证所有英雄，也不推断胜率提升。
- 目标运动用最多 1/240 秒的步长积分，鼠标扫掠和弹丸采用采样间线段近似，并非硬件全采样重建。
- C 的近、远两段各 22.5 秒。近景最后 2.5 秒固定停射结算后再切场景；远景计时结束后只结算已发射弹丸。各候选节奏相同，命中率分母只包含实际发射弹数。
- 本地日期来自设备时钟，不能证明日期未被手动改动。本地存储有配额和浏览器文件源限制，建议及时导出 JSON。
- 高频轨迹按轮保存，仅保留最近 24 轮；汇总成绩保留，最多 180 组、60 批。达到容量时导出备份，再另存 HTML 建新档。
- [KovaaK 的历史 0.0066 预设](https://www.kovaak.com/sensitivity-matcher/)只作理论交叉检查；[MDN 的 Pointer Lock 文档](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_Lock_API)也区分请求选项与平台支持。应用不会据此声称当前游戏客户端或原始输入已获官方验证。

`tests/` 中的脚本和 JSON 是开发验收证据。普通使用只需 HTML，不需要运行这些脚本。

## 只读自动检测更新验收

2026-09-20 增加浏览器环境只读检测。再次用桌面 Chrome 和 Edge，在 `file://`、断网条件下各通过 10 项针对性检查：自动显示环境、重复读取不改写表单/工作副本/存储、DPR 不填写为鼠标 DPI、屏幕变化不覆盖游戏画幅、确认后填写非常见比例、已保存参数保护、刷新恢复非常见比例、数据结构兼容、20 项内置核心自检以及无网络请求/脚本异常。记录在 `tests/environment-test-results.json`，截图和复现脚本同目录。

测试中的屏幕尺寸与 DPR 使用浏览器测试环境模拟，用来验证缩放和不同画幅的分支，不声称测量了显示器物理分辨率、Windows 独立缩放比例或鼠标真实 DPI。原有游戏与硬件人工验证清单继续适用。

## 简单用语更新检查

页面、按钮、提示及使用说明已改用更直白的表达，添加 72 项词语解释；四个主要输入项旁补充了具体填写提示。公式单独折叠，并解释字母的含义。备份文件中的项目名称保留，兼容已有数据。

28 项核心检查通过；桌面 Chrome 和 Edge 各通过页面用语检查，确认词语表可展开、输入提示可见、设置与错误提示正常、结果页和下载按钮正常、20 项页面内自检通过且没有页面异常。另再次通过两款浏览器各 10 项只读检测检查和各 5 项基本功能检查。记录见 `tests/plain-language-test-results.json`、`tests/environment-test-results.json` 和 `tests/final-smoke-results.json`。
