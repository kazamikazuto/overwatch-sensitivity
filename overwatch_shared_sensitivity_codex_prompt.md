# 守望先锋全英雄共用灵敏度评估器：Codex 开发提示词（简化修订版）

请实际实现并测试下述应用，交付能运行的成品，而不是设计稿。本规格替代上一版中按个人主玩英雄选择任务、分别推荐英雄数值或只给候选区间的目标。

## 1. 唯一目标与边界

为我找到**一个尽可能兼顾所有英雄操作风格的、全英雄共用的基础鼠标灵敏度数字 S**。重视整体表现、避免明显短板及跨天稳定性，不追逐单次最高分。

- 只推荐一个基础数值，不生成英雄专属灵敏度，不要求选择本命英雄，不输出多套配置让用户自行挑选。不要将多个英雄的“最佳灵敏度”直接求平均。
- 此数字用于游戏全局／所有英雄的基础鼠标灵敏度字段，以当前客户端的实际名称为准。提醒检查是否存在英雄单独的基础灵敏度覆盖；只统一这一项，不重置按键等其他设置。
- 开镜、技能和形态的相对灵敏度维持用户现有设置，不另外生成数字；这些设置不变是测试前提。共用基础数值不表示不同镜头下的体感完全相同。
- 产品定位是“全英雄操作风格的通用折中”，不是“每个英雄分别达到最优”。没有逐英雄验证时，不宣称已覆盖当前全部英雄和机制。
- 数据不足或候选没有可靠优势时，保留当前基础灵敏度，明确显示“暂时保留原设置”，不要为了生成结果强行换一个数字。

## 2. 成品与界面

交付单个 `overwatch-sensitivity.html`，中文界面，HTML/CSS/JavaScript及必要资源内嵌，离线可用，无 exe、CDN、账号、API Key、安装或构建步骤。最终必须在 Windows 桌面 Chrome/Edge 直接打开验证，包括 file://。

界面只保留三个主要步骤：**设置与校准 → 开始测试 → 一个推荐数值**。原理、公式、诊断数据和详细成绩折叠在“查看详情”，不要做复杂控制台。

必要输入仅为当前基础灵敏度、鼠标 DPI（可选“未知”）、游戏视野/FOV及宽高比；物理距离在校准向导中录入。不要加入手长、握姿、英雄问卷或大量权重滑块。固定设备与 DPI，设备或输入模式改变后提示重新校准。

测试是原创 Aimlabs 风格任务，不冒充 Aimlabs 官方程序或算法。准星固定在中心，鼠标控制第一人称视角；不能把桌面光标点圆圈当作 FPS 视角输入。允许开始时点击一次以取得鼠标锁定，正式测试不需要点击或按住开火。

## 3. 一次性校准：确保数字能对应游戏

使用 Pointer Lock，尝试 `unadjustedMovement:true`，处理不支持、权限拒绝、Esc 和失焦。不得仅因请求参数写上了就宣称已验证原始输入；不支持可靠输入且物理校准也失败时，停止正式推荐。

只累计一条有效鼠标事件输入路径，避免 mousemove/pointermove 双重计数。位移不按帧数放大、不重复乘 DPI、不加隐藏加速、平滑、吸附或瞄准修正。FOV 必须与用户游戏设置对应，水平与垂直定义不能混用；透视投影正确，不能把画布像素尺寸当成视野角度。

优先使用简短的物理校准向导，完成一次后自动保存：

1. 用户在游戏当前灵敏度 S0 下，测量转一圈需要移动的距离 C0（厘米）。用固定远处地标，左右、快慢重复测量；空间不足时可分段累计距离，但提示较高不确定性。
2. 在网页中用尺子辅助移动相同的已知物理距离，重复采样，得到每厘米浏览器位移单位 B。不同方向或速度下明显不一致时提示重测，不可用单一比例掩盖非线性。
3. 在线性检查通过后，使用：
   `deltaYawDeg = browserDeltaX * 360 / (B * C0) * (S / S0)`
   `cm360(S) = C0 * S0 / S`
4. 用另一个已知游戏灵敏度做一次转距核对。最终候选在游戏内再确认转距与保存数值。

保留带出处的理论换算作为详情中的交叉检查，而非当前版本的官方保证。旧规格引用 KovaaK Sensitivity Matcher 的 OW yaw 预设为 0.0066：
`cm360 = 360 * 2.54 / (DPI * 0.0066 * S)`
`deltaYawDeg = rawCountX * 0.0066 * S`
S 是游戏字段中的数字，例如 5，不是 0.05；eDPI 不能作为游戏填写值。DPI 未知时使用物理校准，不虚构 eDPI。理论与实测不一致时明确提示，不能静默忽略。

当前客户端的输入范围、实际保存精度和基础灵敏度覆盖关系须核对，不能沿用未经核实的“最新版本”保证。候选先填入游戏、重新打开设置确认保存值，再按实际保存值测试；按接受精度取整去重，不能最后输出一个未测试的精细小数。向导自动处理计算，不要求用户理解公式。

## 4. 仅四类测试，兼顾不同操作风格

四类都参加综合比较，默认同等重要；不能根据某个英雄偏好关闭整类或改变权重。每个模块默认45秒，模块内部的子阶段按有效时间均分；适应与练习不计分。以下时长与权重是工程起点，不是经过验证的最佳标准。

### A. 精细定位与目标切换

覆盖较小目标、不同切换角度和上下位置。前半段碰到立即触发并换目标，测首次捕获；后半段碰到立即反馈，连续停留达到短阈值才完成，测停稳。默认停留150毫秒是可调设计参数，不是生理标准。

两种成绩分别记录，不把首次碰到说成已经停稳；不把目标消失后的运动判成过冲。以各子阶段固定时间内完成数为主指标，记录超时和失败。综合时先各自与同日原设置相比，再等权合成这一类，不能直接混加不同子任务原始完成数。

### B. 连续跟枪与突然变向

目标不因接触消失，在目标内自动累计有效跟踪时间。包含平滑移动和不规则变向，覆盖水平、垂直、斜向，以及较低和较高角速度；所有候选的条件覆盖相同。无需模拟完整英雄。

主指标为有效跟踪时间/有效测试时间；辅助记录时间加权角误差和跟丢后恢复时间。不能用帧数或事件数代替时间比例。

### C. 弹道提前量

模拟固定节奏的自动发射，目标持续运动。射弹沿发射瞬间的瞄准方向飞行，不追踪目标、不自动修正弹道。使用真实的弹丸运动与碰撞判断命中，不能把“准星碰到当前目标”当成弹道命中。

覆盖可见飞行时间不同的近、远场景，可在同一模块中安排直线飞行与简单下坠两段。所有速度、重力、射速和判定大小固定并保存，标明是原创训练参数，不冒充当前具体英雄的官方数据。首先用不计分预试让用户理解提前量。

主指标为实际命中弹数/有效发射弹数。正式计时结束后，只允许结算已发射弹丸，不再新增发射；等其命中或超时，避免最后发射的弹丸被漏记或错误算失误。此项不评价手动开火时机或蓄力释放，不能声称完整模拟所有弹道英雄。

### D. 大角度转身与重新定位

覆盖左右90°、180°以及上下方向的目标捕获，用对称、明确的方向提示，避免背后目标不可见造成猜测。完成转向后还要重新捕获目标；主指标为固定时间内完成数，失败与超时都保留。

结束时只问一次“转身是否受限／手臂是否不适”，由用户主动反馈抬鼠和撞垫边，不假装网页可自动识别。不能只优化远处小目标而完全忽略此项。

所有模块先在不计分预试确定合适难度，随后冻结。正式比较中不得偷偷改变目标大小、速度、容差、发射节奏或任务组成。保存随机种子，采用难度匹配且在各候选间平衡的路线，不能让某档持续分到更容易的任务。

## 5. 比较流程：自动安排，不让用户折腾

初筛自动生成当前 S0 的 0.8、1、1.25 倍，共三档，按游戏实际接受值取整去重。这只是搜索起点。候选用 A/B/C 标识，可隐藏具体数字，但不宣称双盲。

每档先有不计分适应阶段，再完成四类测试。默认适应60秒，用户仍不适应可延长并记录；不宣称固定时长保证适应完成。每个计分模块内的灵敏度必须固定，不得一边打分一边调整。

每天都测同一组三档，自动轮换顺序；不要今天只测低档、明天只测高档。先提供单日探索记录，再引导至少3个不同日期复测，有条件继续到6天。天数只是流程建议，不自动等于统计充分。一天重复多次不能伪造成多个日期。

测试前用一个简单选项记录状态：较好／一般／较差；结束后可记录不适。不能因正常状态差就删除数据，也不让状态选项直接计算推荐值。

若边缘候选稳定占优，可另开一批相邻候选继续比较；每批仍含当前原设置作为基线。探索和确认数据分开标记，不把不断追分后的最高值宣称为已验证最优。主界面不需要让用户手工控制这一过程。

## 6. 只给一个数字的选取规则

先按日期、任务和匹配难度汇总，再与同日原设置成对比较；四类等权，不能直接相加不同单位的原始分数。可用每类相对于同日原设置的比例变化，再计算四类等权平均。基线为零时标记该项不可判断并提示调整难度、重新收集整组，不靠加极小数造出夸张提升。

选择“总体有重复优势，并且没有明显单项短板”的已测候选，不选择只在某一类高分、牺牲另一类的极端设置。总体相近时优先考虑较差日期的表现和舒适度；没有足够证据区分就保留原设置。

为避免实现成随意的判断，简化版可以采用以下公开的保守工程规则：至少3个有效日期，总体日均相对提升超过3%，且多数日期总体优于原设置；任何一类若多数日期下降、且该类跨日平均下降超过5%，暂不作为通用推荐。百分比和天数是预设的决策门槛，不是统计显著性、已证实的最小可察觉差异或准确率保证；须在详情公开并在确认测试前固定。满足条件的多个候选按四类总体表现比较，接近时优先选择更接近原设置者。分析失败、数据不足或不适反复出现时，不强行给出新推荐。

主结果区只显示：
**推荐共用基础灵敏度：S**
下方显示固定DPI（已知时）、“暂时保留／网页初选／游戏复核通过”的状态，以及一句主要原因。详细任务结果和校准状态可展开，不把候选区间或多个不同英雄数字当作最终答案。

推荐必须来自实际测试过、游戏接受的数值。校准失败时只能显示原设置并说明“尚不能推荐”，不能伪装成已经生成。不要虚构98%准确率，不把跨天稳定性等同于所有英雄胜率提升。

## 7. 游戏内最后确认：少量代表操作，不逐英雄考试

保持同一基础灵敏度，选少量代表性操作检查持续跟枪、精确射击/开镜、弹道提前量、近身转向与位移后的重新定位；英雄可由用户就近选择，不要求逐个测试全部英雄。开镜和技能倍率保持原值。

原设置和候选在同英雄、同场景、同条件下成对比较，记录固定时长表现、是否明显难用及舒适度，不比较不同英雄的原始命中率。网页提供一个简短复核表即可，不复刻游戏，不接入游戏内存，不自动改游戏设置。

这里只决定是否接受“同一个通用数字”。某类出现可重复短板时，退回原设置或测试一个较温和的共用候选；不能因此新增英雄专属数值。一次体验不能证明最优，代表操作复核通过也不能写成已逐个验证所有英雄。游戏成绩与网页成绩分开，不直接混加。

## 8. 必须保留的工程可靠性

- 用 `performance.now()` 统一计时，`requestAnimationFrame` 仅负责画面更新；鼠标完全不动时仍正常倒计时并结束，不按帧数减时间。
- 目标运动、停留与命中使用时间，不受渲染帧率系统性影响。快速扫过目标使用采样间扫掠相交检查，避免漏判；射弹碰撞也检查运动路径，不只检查帧末位置。说明插值是近似，不等于拿到了全部硬件采样。
- 每个目标只能触发一次，新目标不能出生在准星内；一次输入不能连锁命中多个刚生成的目标。目标移动经过静止准星也要正确判定。
- Esc、失焦、切后台或明显卡顿时停止有效计分；正式轮次标记中断并重测，不无声累加或继续。重复开始不得创建重复监听器和时钟。
- 本地保存、JSON导入导出和CSV成绩导出。保存失败明确提醒；演示数据隔离。保留日期、sessionId、设置、校准版本、任务参数、种子、顺序、原始成绩、状态和排除理由；不把每一帧当成独立样本。
- 导入做结构、数值与范围检查，不执行导入的内容；不同设备、任务或校准版本的数据不静默合并。高频轨迹分轮存储，避免无限增长。

验收至少实际检查：计时不依赖鼠标；快速接触不漏不重计；停稳与首次触碰不同；弹道按实际飞行命中、末尾射弹不丢失；相同时间戳输入在不同渲染频率下最终转角一致；不重复乘DPI；输入/保存值校准；中断重测；同日数据不伪装跨日；两档完全同分不强行换参数；单项提高但另一项明显受损不作为通用推荐；断网运行与导入导出。

人工鼠标距离、浏览器输入行为和游戏内回测必须列出人工步骤，不能靠自动化模拟宣称已验证。交付 HTML、简短使用说明及真实的通过／失败／待人工验证清单。不要编造测试记录。

## 9. 实现参考

这些是上一版的技术参考入口，开发时核对其实际内容和兼容性；不代表来源验证了本工具的推荐效果。

- Pointer Lock：https://developer.mozilla.org/en-US/docs/Web/API/Pointer_Lock_API
- 鼠标移动事件单位：https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/movementX
- 动画计时：https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame
- 单调时钟：https://developer.mozilla.org/en-US/docs/Web/API/Performance/now
- OW转向预设：https://www.kovaak.com/sensitivity-matcher/
- 区组设计：https://www.itl.nist.gov/div898/handbook/pri/section3/pri332.htm

以“少操作、一个数字、校准可信、比较公平、结论不过度”为交付原则，不扩展成全英雄数据库或复杂实验平台。
