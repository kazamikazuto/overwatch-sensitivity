# 守望先锋共用灵敏度：使用说明

用 Chrome 或 Edge 打开 **overwatch-sensitivity.html** 就能使用。不用安装软件、启动网站服务或联网。

页面上有“**看不懂某个词？查看简单解释**”，也可以打开同目录的 [术语说明.md](术语说明.md)。例如，FOV 已改成“画面能看到的范围”，校准改成“用尺子测量对齐”，候选改成“测试设置”。

## 先填哪些东西？

- **现在游戏里使用的灵敏度**：照抄游戏设置中的基础鼠标灵敏度数字。页面初始的 5 只是例子。
- **鼠标自身的灵敏度（DPI）**：鼠标软件或鼠标档位中的数字，例如 800。它与游戏灵敏度不同。**不知道就留空，不影响后续测量。**
- **画面能看到的范围（FOV）**：通常数字越大，能看到的周围场景越多。照抄游戏设置中的数字；页面初始的 103 只是例子。
- **游戏画面的宽和高之比**：例如游戏画面 1920 × 1080 对应 16 : 9。按游戏画面填写，不按浏览器窗口填写。

网页会自动查看浏览器和部分屏幕信息，不会修改系统或游戏设置，也不会上传。只有确认游戏画面与屏幕比例相同时，才用“一键填写比例”。显示比例不是鼠标自身的灵敏度，不能拿它代填鼠标档位。

## 按顺序操作

1. **保存设置**：保持同一个鼠标、同一鼠标档位。开镜、使用技能或改变形态时的鼠标速度比例，也保持原值。
2. **在游戏里量距离**：找一个远处固定不动的标记，测视角转一圈时鼠标移动多少厘米。左慢、右慢、左快、右快各一次。再换另一档灵敏度测一次作核对，测完恢复原值。
3. **在网页里量距离**：按说明用尺子完成左右、上下、快慢共 8 次移动。点击开始让鼠标控制视角，按空格开始记录，移动指定距离，再按空格结束；不要中途抬鼠或来回移动。
4. **确认游戏真正保存的数值**：把网页给出的几档数字逐个填进游戏，关掉设置再打开，看它们有没有变。把真正保存下来的数字填回网页。检查是否有英雄单独设置了基础灵敏度，只统一这一项，不重置按键等其他设置。
5. **先练习，再正式比较**：四类任务各练习 12 秒，不计分。选择适合的难度后固定下来。每档正式测试前先适应 60 秒，不够可延长；随后四类任务各 45 秒。鼠标转动视角，屏幕中心小十字是准星，不用点击开火。
6. **换几个日期再测**：每个日期都完成相同几档设置，至少 3 个不同日期，建议 6 天。一天约 12 分钟，不含休息、练习和延长；同一天做多次仍只算一天。成绩太接近、有明显变差的任务或反复不舒服时，程序会保留原设置。
7. **最后回游戏再比一次**：用同一英雄、同一场景、相同时长，比较原设置和网页建议的设置。每类操作各做两轮，再检查保存数值和转一圈的移动距离。反复出现明显变差或不舒服，会退回原值。通过不代表所有英雄都已逐个验证。

四类任务分别是：瞄准小目标、跟着移动目标瞄准、瞄准目标将要到达的位置、转身后重新瞄准。第三类的子弹需要飞行时间，也可能向下落，所以不能总瞄着目标现在的位置。

## 暂停、换设备与保存记录

- 按键盘左上角的 **Esc**、切到其他窗口或页面、改变窗口大小，或明显卡顿超过 **0.25 秒**，都会停止这一轮计分。点击继续后，从头重测这一轮。
- 刚按 Esc 后，浏览器可能暂时不让鼠标再次控制视角。等约 2 秒，再点一次开始；没成功前不会计分。
- 刷新或关掉网页后，不会接着计半轮时间。没完成的一组仍留在记录里，但不参与正式比较；重新开始当天完整一组即可。
- 换鼠标、改鼠标档位、浏览器或系统输入方式有变化后，要重新测量。若只是难度不合适，可在测试页详情里选择“调整难度并重新练习”；旧成绩会单独保存，不混进新一套比较。
- 在结果页详情里下载 **完整备份（JSON）**，可以保存全部设置和成绩，以后恢复。恢复时会先检查文件，再请你确认是否替换现有记录。
- **成绩表（CSV）** 是给 Excel 等表格软件看的，不能替代完整备份。详细鼠标移动记录只保留最近 24 轮，汇总成绩继续保留。
- 保存失败会明确提醒，请立即下载完整备份。换浏览器、移动网页文件或清理浏览数据，可能会找不到旧记录。

## 仍需要你亲自验证的部分

网页不能替你量鼠标真实距离，也不能证明系统没有改动鼠标输入。游戏当前允许的数值、能保存几位小数、英雄单独设置、画面范围与比例、手臂是否舒服，都需要你确认。

自动检查的结果见 [测试报告.md](测试报告.md)。报告里模拟出来的鼠标数据和成绩，只用于检查程序，不能当成你的真实游戏成绩。报告中的开发用词也在 [术语说明.md](术语说明.md) 中解释了。

旧资料中的计算公式只用来额外核对，不能代替实测。程序会自己计算，不要求你记住公式；如果实测与旧公式相差超过 10%，页面会提醒。
